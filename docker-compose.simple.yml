version: '3.8'

services:
  # Backend - API apenas
  backend:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DB_URL=sqlite:////data/backend_data.db
      - CLIENT_BASE_URL=${CLIENT_BASE_URL:-https://hakimfarma.devsible.com.br}
      - ALLOWED_ORIGIN=${ALLOWED_ORIGIN:-https://hakimfarma.devsible.com.br}
      - SESSION_VALIDITY_HOURS=${SESSION_VALIDITY_HOURS:-4}
    volumes:
      - ./data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Frontend - Nginx servindo arquivos estáticos
  frontend:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./catalogo.html:/usr/share/nginx/html/catalogo.html:ro
      - ./admin-instancias.html:/usr/share/nginx/html/admin-instancias.html:ro
      - ./admin-pedidos.html:/usr/share/nginx/html/admin-pedidos.html:ro
      - ./demo.html:/usr/share/nginx/html/index.html:ro
      - ./public:/usr/share/nginx/html/public:ro
      - ./nginx.simple.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  data: