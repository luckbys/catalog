version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DB_URL=sqlite:////data/backend_data.db
      - CLIENT_BASE_URL=${CLIENT_BASE_URL:-https://chatbot-catalog.zv7gpn.easypanel.host}
      - ALLOWED_ORIGIN=${ALLOWED_ORIGIN:-https://chatbot-catalog.zv7gpn.easypanel.host}
      - SESSION_VALIDITY_HOURS=${SESSION_VALIDITY_HOURS:-4}
      - SUPABASE_URL=${SUPABASE_URL:-https://chatbot-supabase1.zv7gpn.easypanel.host}
      - SUPABASE_KEY=${SUPABASE_KEY}
      - EVOLUTION_API_URL=${EVOLUTION_API_URL}
      - EVOLUTION_API_KEY=${EVOLUTION_API_KEY}
      - EVOLUTION_INSTANCE_NAME=${EVOLUTION_INSTANCE_NAME}
      - WHATSAPP_PHONE=${WHATSAPP_PHONE}
    volumes:
      - ./data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  frontend:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./catalogo.html:/usr/share/nginx/html/catalogo.html
      - ./demo.html:/usr/share/nginx/html/demo.html
      - ./nginx.prod.conf:/etc/nginx/conf.d/default.conf
      - ./ssl:/etc/nginx/ssl:ro
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  data: